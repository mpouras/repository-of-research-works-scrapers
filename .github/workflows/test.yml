name: Hello World

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Run Hello World in Node.js
        run: |
          node -e "console.log('Hello world')"